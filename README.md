This is keiba notification to Line.

# exec in local

```
npx @google-cloud/functions-framework --target=sendLine
```

access to http://localhost:8080/

# deploy

When merge to main branch, deploy to Cloud Run.
